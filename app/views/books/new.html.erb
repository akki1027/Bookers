<body>
	<h2 class="new-h2">New Book</h2>

	<%= form_for(@book) do |f| %>
		<div class="field-area">
			<h4>Title</h4>
			<%= f.text_field :title %>
		</div>

		<div class="field-area">
			<h4>Body</h4>
			<%= f.text_area :body %>
		</div>

			<%= f.submit 'Create Book' %>

	<% end %>
</body>
<!-- 一応完成 -->