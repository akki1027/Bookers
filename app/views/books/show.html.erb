<body>
	<strong>Title:</strong>
	<p><%= @book.title %></p>
	<strong>Body:</strong>
	<p><%= @book.body %></p>

	<%= link_to "edit", edit_book_path(@book) %>
	<%= link_to "back", edit_book_path(@book) %>
</body>

<!-- 未完成　indexができてから直す-->